<li>
  <%= gravatar_for user, size: Settings.avatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %> | <%= link_to t("static_pages.users.delete_user"), user, method: :delete, data: { confirm: t("static_pages.users.confirm_del") } %>
  <% end %>
</li>
